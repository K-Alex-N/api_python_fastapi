[tool.ruff]
ignore = ["F731"]
exclude = [
    ".git",
    "__pycache__",
    ".venv",
    ".not_for_git",
    "welcome-page",
]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A"]


[tool.pyright]
venvPath = "."
venv = ".venv"
exclude = [".venv", ".not_for_git", "welcome-page"]

